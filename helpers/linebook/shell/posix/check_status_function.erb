Adds the check status function.
()
  @check_status = true
--
check_status () {
  expected=$1; actual=$2; lineno=$3

  if [ $actual -ne $expected ]
  then
    echo "[$actual] $0:$lineno"
    exit $actual
  fi

  return $actual
}
